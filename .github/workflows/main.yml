name: SFDX Test Run on Push

on: [push]

jobs:
  test:
  
    runs-on: ubuntu-latest
    
    steps:
      - uses: sfdx-actions/setup-sfdx@v1
        with:
          sfdx-auth-url: force://${{secrets.CLIENT_ID}}:${{secrets.CLIENT_SECRET}}:${{secrets.REFRESH_TOKEN}}@rogalski-dev-ed.my.salesforce.com
      - name: sfdx-actions
        run: | 
          echo "switched `pwd`"
          cd "/home/runner/work/flowRefreshToken/"
          echo "switched `pwd`"
          cd "/home/"
          echo "e? `ls -a`"
          cd "/home/runner/work/flowRefreshToken/force-app"
          echo "switched `pwd`"
          sfdx force:source:deploy -x manifest/package.xml -l RunLocalTests
